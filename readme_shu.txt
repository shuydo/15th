https://git-scm.com/book/en/v2/Getting-Started-First-Time-Git-Setup

git config --global user.email "you@example.com"
git config --global user.name "Your Name"

ideas:
-----------------
- 4 defense from complete pazle after shuffle add veriication and addition shuffle
- add finger poinder above tiles, who possible to move
- add mute chackbox
- add timer
- add Guide, who may hide by chackbox
- add hiding settings color...
- add save params & records in local storage
- add erase record.list